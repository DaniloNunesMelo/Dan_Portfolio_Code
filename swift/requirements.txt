# Runtime dependencies (all stdlib – no external packages needed for core logic)
# xml.etree.ElementTree, csv, re, dataclasses are all built into Python 3.8+

# Development / CI dependencies
pytest>=8.0
pytest-cov>=5.0
ruff>=0.4
